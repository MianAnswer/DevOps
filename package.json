{
  "name": "devops",
  "version": "1.0.0",
  "description": "A project to practice setting up a software system to production readiness",
  "license": "ISC",
  "author": "Waqas Answer",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "docker:build": "docker build --target base -t devops:latest .",
    "docker:run": "docker run -d --name devops -p 3000:3000 devops:latest",
    "docker:stop": "docker stop devops && docker rm devops",
    "docker:start": "npm run docker:build && npm run docker:run",
    "start": "node index.js",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js --detectOpenHandles --forceExit"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "jest": "^30.3.0",
    "supertest": "^7.2.2"
  }
}
